# Ticker Research Views

Status: Phase 2 first vertical slice implemented for visual review.

## Information architecture

- Overview remains the quick read. Research views do not repeat its price chart, final grade, technical gauges, metric ribbon, or relationships preview.
- Research navigation keeps the primary destinations visible in one horizontally scrollable secondary bar. Only Financials, Signals, and More use compact anchored disclosures.
- Every Research, Overview, and Relationships link preserves `?lens=trade|short|medium|long`.
- The selected Lens changes initial priority and defaults; it never hides the remaining evidence.

## First vertical slice

### Company Profile / Fund Profile

- Company Profile prioritizes the business description, then profile facts and secondary identifiers.
- Fund Profile replaces company language with holdings, sector exposure, distributions, and risk.
- Missing descriptive or identifier fields use a compact `Data pending` state without fabricated copy.

### Fundamentals

- Equity themes are Valuation, Growth, Profitability, Financial health, and Shareholder return.
- Fund themes are Portfolio, Exposure, Valuation, Distributions, and Risk.
- Current canonical metric rows remain visible in every Lens; Lens only changes their order and visual priority.
- Historical trend slots are reserved as `Data pending` until a canonical series contract exists.

### Financial Statements

- Income Statement, Balance Sheet, and Cash Flow are stable URL states through `?statement=`.
- Annual and Quarterly are stable URL states through `?period=`.
- The current summary snapshot is explicitly separate from a complete statement series.
- Statement trajectories, comparative periods, growth, units, and restatements remain `Pending integration`; no values or dates are simulated.
- Mobile exposes the selected period as the primary column rather than making horizontal scrolling the main interaction.

## Data boundaries

- Runtime product data comes only from finance-backend through `lib/backend.ts`.
- Current views use `/tickers/:ticker/summary` and `/tickers/:ticker/profile` through existing server helpers.
- No API route, backend contract, scoring logic, provider fallback, Yahoo path, or Supabase path is added.
- Missing statement and full-profile fields are documented in `docs/api/requested-endpoints.md` as contract gaps, not available functionality.

## Shared UI

- `ResearchViewShell` owns the factual page heading, ticker context, coverage label, Perspective Dial, and development-only ad placement.
- `StockResearchNav` owns keyboard-operable horizontal navigation, active-destination visibility, Lens-preserving links, compact disclosures, Escape/outside close, and mobile scrolling. It does not display or explain the current Perspective.
- `ResearchViews.module.css` supplies document-flow chapters, metric rows, statement controls, table behavior, zoom-safe layouts, and reduced-motion handling.

## Valuation History and Ownership & Capital slice

Status: implemented and visually approved for commit on 2026-07-22. The slice uses the existing summary/profile payloads only. Current valuation evidence is limited to the canonical current trailing P/E and its available metadata; current ownership evidence is limited to market cap, shares outstanding, currency, and reporting period. Historical multiples, ownership composition, holders, capital changes, debt, cash, and enterprise-value terms remain intentional integration states documented in `docs/api/requested-endpoints.md`.

### Final validation record

- `npm run verify`: passed; 0 errors and 5 existing lint warnings.
- Focused browser suite: 2/2 passed.
- Combined relevant browser suite: 12/12 passed.
- Legacy redirect check: passed for `signal-history`, `performance`, `holdings-dividends`, and `financials/fund-profile`.
- `git diff --check`: passed.
- `npm run build`: blocked outside this slice. Turbopack remained at `Creating an optimized production build ...` until the 90-second timeout. A direct Webpack diagnostic exposed `NextFontError: Failed to fetch Caveat from Google Fonts`; the same failure was reproduced from a clean `HEAD` checkout, with no pending Next/build process afterwards. No functional workaround was added.

### Screenshot evidence

The final browser captures are generated by `e2e/ticker-research-valuation-ownership.live.spec.ts` and remain under the ignored `test-results/` artifact directory for this slice:

- Valuation: AAPL Trade desktop, Trade mobile, and Long-term pending desktop.
- Ownership: AAPL Long-term desktop and mobile.
- Fund Structure: QQQ desktop.
- Responsive validation: QQQ at effective 200% zoom.

Representative artifact paths:

- `test-results/ticker-research-valuation--c4045-ic-frequency-and-Lens-state-chromium/phase2-valuation-aapl-trade-desktop.png`
- `test-results/ticker-research-valuation--c4045-ic-frequency-and-Lens-state-chromium/phase2-valuation-aapl-trade-mobile.png`
- `test-results/ticker-research-valuation--c4045-ic-frequency-and-Lens-state-chromium/phase2-valuation-aapl-long-pending-desktop.png`
- `test-results/ticker-research-valuation--1c102-serve-asset-aware-semantics-chromium/phase2-ownership-aapl-long-desktop.png`
- `test-results/ticker-research-valuation--1c102-serve-asset-aware-semantics-chromium/phase2-ownership-aapl-long-mobile.png`
- `test-results/ticker-research-valuation--1c102-serve-asset-aware-semantics-chromium/phase2-ownership-qqq-fund-structure.png`
- `test-results/ticker-research-valuation--1c102-serve-asset-aware-semantics-chromium/phase2-ownership-qqq-200-zoom.png`

## Signals & Indicators and Earnings & Events slice

Status: closed and visually approved. `/stocks/[ticker]/signals` is the single Signals & Indicators surface. `/stocks/[ticker]/indicators` is compatibility-only and permanently redirects to `/signals`, preserving `lens` and `family`. No parallel indicator page exists.

### Confirmed fields used

- Current signal: direction, signal date, reported horizon, current price when available, and `coverage.hasSignals`.
- Signal history: validated `id`, date, direction, and reported horizon from the existing `/signals/history/:ticker` helper.
- Price timeline: existing OHLC date and close rows from `/tickers/:ticker/ohlc`.
- Technical evidence: the existing `buildTechnicalSummary` implementation over available OHLC, keeping Summary, Oscillators, and Moving Averages together.
- Equity next earnings: date, time when supplied, fiscal period, EPS estimate, revenue estimate, and as-of metadata from the existing summary.
- Fund event context: existing ex-distribution/ex-dividend field only when supplied by the profile normalizer.

### Semantics deliberately rejected

- `prob_side`, `signal_strength`, returns, and episode state are not rendered as confidence, accuracy, probability of success, expected return, or performance.
- Signal rows are not silently deduplicated and are not grouped into inferred regimes.
- OHLC aggregation labels are existing technical implementation states, not new canonical backend horizons. No `timeframe` URL parameter was introduced; chart range and reported signal horizon remain separate.
- Earnings surprise charts, price reactions, restatements, timezone, certainty, source, guidance, and event impact are not inferred.

### Pending modules

- Regime History remains `Pending integration` because no canonical regime-history contract exists.
- Historical indicator series, liquidity fields, event feed, corporate actions, filings, guidance, and fund-specific event feeds remain `Pending integration` or `Partial coverage`.
- Earnings history is shown only when server normalization finds no duplicate date/fiscal-period keys; otherwise it remains a partial-coverage state.

### Final slice decisions

- Signal History remains collapsed by default so the price timeline and current technical evidence lead the page; validated observations remain available without making the first view a long table.
- `family=summary|oscillators|moving-averages` is the shareable technical subview state. Chart range is selected from the existing Lens defaults and is not exposed as a signal horizon.
- Equity Events uses only safe next-earnings and ex-dividend fields. Fund Events uses fund-specific labels and does not show corporate earnings, guidance, shareholder meetings, or filings as if they applied to the fund.
- Current signal and event rows remain factual. No client-side score, performance, regime, surprise, timezone, certainty, source, or price-impact inference was added.
- The approved screenshot matrix covers AAPL Trade and Long-term Signals, partial/no-signal equity, AAPL Events with history, QQQ Fund Events, mobile, submenu state, and effective 200% zoom.

### Screenshot evidence

The focused captures are generated by `e2e/ticker-research-signals-events.live.spec.ts` and remain under `test-results/`:

- Signals: AAPL Trade desktop, Long-term desktop, mobile, partial-equity state, submenu, and 200% zoom.
- Events: AAPL desktop/mobile/history, QQQ Fund Events, and partial coverage.

Representative artifact paths:

- `test-results/ticker-research-signals-ev-2498e-the-legacy-indicators-route-chromium/phase2-signals-aapl-trade-desktop.png`
- `test-results/ticker-research-signals-ev-2498e-the-legacy-indicators-route-chromium/phase2-signals-aapl-long-desktop.png`
- `test-results/ticker-research-signals-ev-2498e-the-legacy-indicators-route-chromium/phase2-signals-aapl-mobile.png`
- `test-results/ticker-research-signals-ev-2498e-the-legacy-indicators-route-chromium/phase2-signals-aapl-200-zoom.png`
- `test-results/ticker-research-signals-ev-2498e-the-legacy-indicators-route-chromium/phase2-signals-submenu-active.png`
- `test-results/ticker-research-signals-ev-e17b6--asset-aware-partial-states-chromium/phase2-signals-partial-equity.png`
- `test-results/ticker-research-signals-ev-e17b6--asset-aware-partial-states-chromium/phase2-events-aapl-desktop.png`
- `test-results/ticker-research-signals-ev-e17b6--asset-aware-partial-states-chromium/phase2-events-aapl-mobile.png`
- `test-results/ticker-research-signals-ev-e17b6--asset-aware-partial-states-chromium/phase2-events-aapl-history-partial.png`
- `test-results/ticker-research-signals-ev-e17b6--asset-aware-partial-states-chromium/phase2-events-qqq-fund.png`

## AI Research and Methodology slice

Status: implemented and visually approved on 2026-07-22. This slice is limited to the ticker AI capability preview and the editorial Methodology destination.

### AI Research

- `/stocks/[ticker]/ai-research` preserves `?lens=` and uses the shared Research shell and navigation.
- The page is a capability preview while the ticker view is not connected to the AI integration.
- The question field, Ask control, and suggested questions are explicitly disabled; the page does not call `/api/ai-analyst` or generate simulated answers.
- Response and citation geometry is reserved for the final integration, with a single discreet `Preview` state rather than repeated empty panels.
- Viewer access is read through the existing Clerk/Stripe billing helper. `Plan required` is an access state prepared for the future integration; the current response state remains integration-pending.
- Session history is intentionally absent until a ticker-specific session contract is approved.

### Methodology

- `/stocks/[ticker]/methodology` preserves `?lens=` and presents a document-flow editorial layout with a compact on-page index.
- The page distinguishes the canonical score, reported model signal, and OHLC-derived technical read without adding new scoring or signal semantics.
- It documents finance-backend as the product-data source, shows how timestamps and frequency are treated, and explains Available, Partial coverage, Preview, Unavailable, and Plan required states.
- Equity and fund applicability is described separately; corporate concepts are not implied for funds.
- Disclosures use only confirmed public destinations: `/product#limits` and `/faq`. No disclosures route was invented.

### Final validation and screenshot evidence

- `npm run verify`: passed; 0 errors, 5 existing lint warnings, and 11 tests passed.
- Focused browser QA: 2/2 passed with live ticker coverage.
- `git diff --check`: passed.
- The AI preview test also verifies that no request is made to `/api/ai-analyst`.
- Screenshots are generated by `e2e/ticker-research-ai-methodology.live.spec.ts` and remain under the ignored `test-results/` directory:

  - `test-results/ticker-research-ai-methodo-8c14b-op-mobile-coverage-and-zoom-chromium/phase2-ai-research-aapl-desktop.png`
  - `test-results/ticker-research-ai-methodo-8c14b-op-mobile-coverage-and-zoom-chromium/phase2-ai-research-aapl-mobile.png`
  - `test-results/ticker-research-ai-methodo-8c14b-op-mobile-coverage-and-zoom-chromium/phase2-ai-research-partial-coverage.png`
  - `test-results/ticker-research-ai-methodo-8c14b-op-mobile-coverage-and-zoom-chromium/phase2-ai-research-200-zoom.png`
  - `test-results/ticker-research-ai-methodo-27873--at-desktop-mobile-and-zoom-chromium/phase2-methodology-aapl-desktop.png`
  - `test-results/ticker-research-ai-methodo-27873--at-desktop-mobile-and-zoom-chromium/phase2-methodology-aapl-mobile.png`
  - `test-results/ticker-research-ai-methodo-27873--at-desktop-mobile-and-zoom-chromium/phase2-methodology-200-zoom.png`

## Phase 3 Relationships

Status: reduced-scope implementation in progress. The only live destination is `/stocks/[ticker]/relationships`.

### Scope

- Relationships is a single Relationship Evidence view, not a collection of Peers, Sector, Macro, Structural, or Business Relationships pages.
- Runtime relationship data comes exclusively from `GET /relationships/:ticker` through `lib/relationships.ts`.
- The view uses the backend-returned layers only: residual co-movement, theme relationship, directional relationship, market co-movement, and probable spurious relationships.
- `strength` is an optional raw field. Missing values remain absent and are not rendered as zero.
- `confidence` is preserved as optional data but is not shown until its scale and meaning are confirmed.
- Lead/lag records are presented as `Directional relationship`; the UI does not draw causal arrows or use causal language.
- Theme records use the neutral `Theme relationship` label until the backend definition is confirmed.
- The map is supplementary. Desktop uses a bounded graph when the relationship has both a valid node identity and raw strength; mobile is list-first.
- Weak/probable-spurious records remain closed by default and are shown only under their backend classification.

### URL state and coverage

- `?lens=` is preserved by the Research shell and navigation.
- `?layer=residual|theme|leadLag|market` selects a supported relationship layer.
- `?window=126|252` preserves the numeric backend window only; neither value is labelled as a human time horizon until its unit and frequency are confirmed.
- If no supported layer/window has enough identified relationships, the page shows one compact Partial coverage state.
- Equity and fund language remains asset-aware. Fund holdings and sector weights continue to belong to Fund Profile/Fund Structure and are not converted into relationship edges.

### Data boundaries

- No client-side relationship, correlation, persistence, beta, influence, lead/lag, or macro calculations are added.
- No unknown graph nodes are manufactured to complete the map.
- Rows without a usable symbol are rejected by the normalizer; rows without strength can remain readable in the list but cannot create a visually weighted graph edge.
- The page explains that observed association does not establish causality, prediction, influence, or a business relationship.

### Phase 3 validation and screenshot matrix

- `npm run verify`: passed; 0 errors, 5 existing lint warnings, and 11 tests passed.
- Focused Relationships browser QA: 4/4 passed.
- Focused 320px mobile check: passed.
- Existing ticker live regression suite: 6/6 passed.
- `git diff --check`: passed.
- The focused matrix covers AAPL desktop with each available layer, AAPL mobile at 390px and 320px, QQQ partial fund coverage, 0005.HK partial equity coverage, reduced motion, keyboard focus, and shareable `lens`, `layer`, and `window` state.
- Existing representative artifacts remain under `test-results/`, including `ticker-page.live-live-tick-bb3a2-op-and-list-first-on-mobile-chromium/aapl-relationships-desktop.png`, `aapl-relationships-mobile.png`, and `ticker-page.live-live-tick-ebc47-on-without-a-current-signal-chromium/0005-partial-equity-desktop.png`.
- Browser output still includes the pre-existing PerspectiveDial hydration warning and the expected Motion reduced-motion notice; neither is introduced by the Relationships implementation.
